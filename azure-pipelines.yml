trigger:
- main
pool: pool
stages:
  - stage: terraforminit
    displayName: "TERRAFORM INIT"
    jobs:
      - job: terraforminit
        displayName: "TERRAFORM INIT"
        pool: pool
        steps:
        - task: TerraformInstaller@1
          inputs:
            terraformVersion: 'latest'
        
        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'init'
            workingDirectory: '$(System.DefaultWorkingDirectory)/parent'
            backendServiceArm: 'ser'
            backendAzureRmResourceGroupName: 'vishnu-rg'
            backendAzureRmStorageAccountName: 'vishnu-stg'
            backendAzureRmContainerName: 'vishnu-cont'
            backendAzureRmKey: 'terraform.tfstate'

        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'plan'
            workingDirectory: '$(System.DefaultWorkingDirectory)/parent'
            environmentServiceNameAzureRM: 'ser'

  - stage: terraformscan
    displayName: "TERRAFORM SCAN"
    jobs:
      - job: terraformscan
        steps:
        
        - task: tfsec@1
          inputs:
            version: 'v1.26.0'
            dir: '$(System.DefaultWorkingDirectory)'



  - stage: terraforminitapply
    displayName: "TERRAFORM INIT APPLY"
    jobs:
      - job: terraforminitapply
        displayName: "TERRAFORM INIT APPLY"
        pool: pool
        steps:
        
        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'init'
            workingDirectory: '$(System.DefaultWorkingDirectory)/parent'
            backendServiceArm: 'ser'
            backendAzureRmResourceGroupName: 'vishnu-rg'
            backendAzureRmStorageAccountName: 'vishnu-stg'
            backendAzureRmContainerName: 'vishnu-cont'
            backendAzureRmKey: 'terraform.tfstate'
        
        - task: TerraformTask@5
          inputs:
            provider: 'azurerm'
            command: 'apply'
            workingDirectory: '$(System.DefaultWorkingDirectory)/parent'
            commandOptions: '-auto-approve'
            environmentServiceNameAzureRM: 'ser'